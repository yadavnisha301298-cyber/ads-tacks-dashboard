/// The large gradient promotional card at the top of the dashboard.
class HeroProject {
  const HeroProject({
    required this.tag,
    required this.title,
    required this.description,
    required this.ctaLabel,
  });

  final String tag;
  final String title;
  final String description;
  final String ctaLabel;
}
